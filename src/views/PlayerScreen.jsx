import React from "react";
import { View, Text } from "react-native";

export default function PlayerScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Reproductor (Pendiente)</Text>
    </View>
  );
}
